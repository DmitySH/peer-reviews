Привет! Это ...

Использовал NuGets:
Newtonsoft.JSON
CsvHelper
Extended.Wpf.Toolkit
System.Windows.Interactivity.WPF

Реализовано три доп:
Рандом генерация
Доп характеристики
Сортировка

ВНИМАНИЕ!

При компиляции с открытым окном MainWindow будут выскакивать Message Boxes, потому что он пытается десериализовать склад
в дизайнере. Это не ошибка. Чтобы убедиться можно компилировать с другим открытым в VS файлом (не окно).

Файл сохраняется всегда, когда нужно (то есть при закрытии приложения и при смене склада) или вручную,
поэтому данные всегда сохранены и потерять их таким образом нельзя.
Единственное, если у складов одинаковые названия, то старый перезаписывается и при выходе из приложения склад сохраняется в файлом с именем - 
названием склада. При повтороном запуске программы открывается последний сохраненный склад(то есть склад, что был при выходе).

Реализация с катлогами и папками по отедльности(то есть что у секции нет товаров и секций, а есть что-то только одно) является корректной

Отчет CSV пригоден для чтения в Excel. Например так (Excel): Выбираем первый столбец полностью -> данные -> текст по столбцам -> далее
-> отмечаем запятую -> далее -> готово

Так же была штука, что из-за .NET5 (на коем написан проект) он на некоторых устройствах не будет компилировать вообще.
Просто нажимаем запустить последний исправный вариант и все работает.

Не недо трогать файлы внутри самого проекта =))) очевидно, так можно любую программу сломать.

ВНИМАНИЕ!

Кажется все, что надо было сказать. Удачной проверки =)

P.S. Впервые попробовал реализовать MVVM, за него строго не судите:)
